const menuBtn=document.querySelector('[data-menu]');const nav=document.querySelector('[data-nav]');
if(menuBtn&&nav){menuBtn.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')))}
document.querySelectorAll('[data-destination]').forEach(a=>a.addEventListener('click',()=>{const s=document.querySelector('select[name="destination"]');if(s)s.value=a.dataset.destination}));
const booking=document.querySelector('[data-booking]');if(booking){booking.addEventListener('submit',async e=>{e.preventDefault();const obj=Object.fromEntries(new FormData(booking).entries());const status=booking.querySelector('[data-book-status]');status.textContent='Sending…';const r=await fetch('/booking',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(obj)});if(r.ok){booking.reset();status.textContent='Booking request sent successfully.'}else{status.textContent='Could not send the request. Please call or WhatsApp us.'}})}
